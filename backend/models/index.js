const Emergency = require('./emergency.model');
const Admin = require('./admin.model');
const EmergencyLog = require('./emergencyLog.model');
const ResponseUnit = require('./responseUnit.model');

module.exports = {
  Emergency,
  Admin,
  EmergencyLog,
  ResponseUnit
};
